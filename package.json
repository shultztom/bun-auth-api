{
  "name": "bun-auth-api",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "start:dev": "bun run --hot index.ts",
    "migrate:dev": "npx prisma migrate dev --name init"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.3.1",
    "prisma": "^5.3.1"
  }
}